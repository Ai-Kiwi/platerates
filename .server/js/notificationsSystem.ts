async function sendNotification(userId : string,data : Array<any>) {
    //this function will be ran even if they have notification for it disabled


    try{

    }catch (err){
        return {
            success : false,
        }
    }
}